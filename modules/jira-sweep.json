{
  "id": "jira-sweep",
  "display_name": "Jira Sweep",
  "description": "Read scoped Jira queries and, when narrowly enabled, apply approved create, update, comment, or transition operations with duplicate checks.",
  "required_connectors": ["atlassian"],
  "read_sources": [
    {"type": "jira", "mode": "read-only", "scope_config_key": "modules.jira-sweep.queries[].scope_id"}
  ],
  "write_targets": [
    {
      "type": "jira",
      "requires_approval": true,
      "permission_key": "permissions.jira_writes_enabled",
      "scope_config_key": "modules.jira-sweep.writes.scope_ids"
    }
  ],
  "config_schema": {
    "type": "object",
    "required": ["enabled", "connector_configured", "queries"],
    "properties": {
      "enabled": {"type": "boolean"},
      "connector": {"type": "string"},
      "connector_configured": {"type": "boolean"},
      "queries": {"type": "array"},
      "writes": {"type": "object"}
    }
  },
  "onboarding": {
    "question": "Which scoped Jira queries should the close read?",
    "default_for_profiles": [],
    "setup_questions": [
      "Is an Atlassian/Jira connector authenticated?",
      "Which JQL query should be used for each profile scope?",
      "What maximum number of issues should each query return?",
      "Should Jira own team, delegated, multi-step, or acceptance-criteria actions?",
      "Which scope-bound project key, issue type, assignee, and lifecycle operations are allowed?"
    ],
    "connector_probes": ["Read the authenticated Jira identity without listing issue content."],
    "required_artifacts": [],
    "optional_artifacts": [],
    "config_keys": [
      "modules.jira-sweep.connector_configured",
      "modules.jira-sweep.queries",
      "modules.jira-sweep.writes",
      "permissions.jira_writes_enabled"
    ]
  },
  "enabled_by_default": false,
  "proposal_output_type": "jira_capture_candidates"
}
