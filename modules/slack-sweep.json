{
  "id": "slack-sweep",
  "display_name": "Slack Sweep",
  "description": "Read recent Slack mentions, DMs, and selected channels for asks, commitments, and follow-ups.",
  "required_connectors": [
    "slack"
  ],
  "read_sources": [
    {
      "type": "slack",
      "mode": "read-only"
    }
  ],
  "write_targets": [],
  "config_schema": {
    "type": "object",
    "required": [
      "enabled"
    ],
    "properties": {
      "enabled": {
        "type": "boolean"
      },
      "window": {
        "type": "string"
      },
      "channels": {
        "type": "array"
      }
    }
  },
  "onboarding": {
    "question": "Should close-day read Slack mentions, DMs, or channels for asks and follow-ups?",
    "default_for_profiles": [],
    "setup_questions": [
      "Is Slack connected in Codex?",
      "Which channels or DMs should be included beyond mentions?"
    ],
    "connector_probes": [
      "Probe Slack channel access when the connector is available."
    ],
    "required_artifacts": [],
    "optional_artifacts": [
      "List of project or account channels to scan."
    ],
    "config_keys": [
      "modules.slack-sweep.enabled",
      "modules.slack-sweep.window",
      "modules.slack-sweep.channels"
    ]
  },
  "enabled_by_default": false,
  "proposal_output_type": "slack_capture_candidates"
}
