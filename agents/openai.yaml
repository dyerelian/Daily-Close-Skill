interface:
  display_name: "Close Day"
  short_description: "Modular EOD close-out and CRM proposal workflow"
  default_prompt: "Use $close-day to close my day, include CRM follow-ups, and present one approval proposal before writing anything."

policy:
  allow_implicit_invocation: true
