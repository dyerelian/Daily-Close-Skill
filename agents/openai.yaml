interface:
  display_name: "Close Day"
  short_description: "Modular EOD close-out with gated Jira and CRM writes"
  default_prompt: "Use $close-day to close my day, list any proposed new Jira tickets first for approval, and present one consolidated proposal before writing anything."

policy:
  allow_implicit_invocation: true
