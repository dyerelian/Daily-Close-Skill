interface:
  display_name: "Close Day"
  short_description: "Route and close scoped work across daily systems"
  default_prompt: "Use $close-day to review my selected profile, route actions to their configured primary systems, and present the scoped proposal before writing anything."

policy:
  allow_implicit_invocation: true
