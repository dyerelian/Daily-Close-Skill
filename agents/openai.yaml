interface:
  display_name: "Close Day"
  short_description: "Multi-profile EOD close and planning workflow"
  default_prompt: "Use $close-day to close my selected personal or organization profile and present the scoped proposal before writing anything."

policy:
  allow_implicit_invocation: true
